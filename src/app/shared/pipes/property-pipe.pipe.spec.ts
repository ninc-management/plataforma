import { PropertyPipe } from './property-pipe.pipe';

describe('PropertyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
